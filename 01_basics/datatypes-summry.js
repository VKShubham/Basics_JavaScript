// primitive data type

// 7 types  :- String, BigInt, Number, Boolean, null, Undefined

// Refrence type (Non - primtive type)

// Array, Function, Object

// comprision 

let a = Symbol('123');
let b = Symbol('123');

console.log(a === b); // it return false because it(Symbol) creates always new object in heap memory